import * as Cookie from './cookie.js';


// Callbacks
Cookie.checkCookie('favorite_image');

window.addEventListener('keypress', function(e) {
    if(e.key === 'Enter') {
      const search = document.getElementById('search').value;

      /**
       * if search.includes(against array title)
       * append created elements with any that match 
       * also add search result at top
       * also add button to stop search results
       */
    }
});
  







